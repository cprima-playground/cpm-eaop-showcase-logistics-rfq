[project]
name = "rfq-policy-evaluation"
version = "0.1.0"
description = "Standalone Cedar PDP test harness for the RfQ repricing showcase scenarios."
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "pyyaml>=6.0",
]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
package = false
